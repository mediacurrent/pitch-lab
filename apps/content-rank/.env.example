# CMS base URL for fetching content rank instances.
# Local: http://localhost:3001
# Production: your deployed CMS URL
# CMS_URL=http://localhost:3001

# Secret for migration session API (must match MIGRATION_SESSION_API_SECRET on the CMS).
# Used to create/update migration review sessions tied to email.
# MIGRATION_SESSION_API_SECRET=your-shared-secret
