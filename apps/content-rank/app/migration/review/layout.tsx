import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Content Migration Analyzer',
}

export default function ReviewLayout({ children }: { children: React.ReactNode }) {
  return children
}
