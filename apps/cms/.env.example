# Payload CMS
# Generate a random string (min 32 characters) for production
PAYLOAD_SECRET=your-secret-key-min-32-chars-long

# Production URL (no trailing slash). On Vercel this is auto-set from VERCEL_URL; set for custom domains.
# PAYLOAD_PUBLIC_SERVER_URL=https://pitch-lab-cms.vercel.app

# MongoDB
# Local: mongodb://localhost:27017/payload-turbo
# Atlas:  mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
# For Vercel + Atlas: (1) Use mongodb+srv:// (2) URL-encode password if it has special chars
# (3) In Atlas Dashboard → Network Access, add 0.0.0.0/0 so Vercel's IPs can connect
# Use either variable name (Payload checks both)
DATABASE_URI=mongodb://localhost:27017/payload-turbo
# MONGODB_URI=mongodb://localhost:27017/payload-turbo

# Content Rank: override CSV upload directory (Payload stores CSVs in apps/cms/csv-uploads by default)
# Only needed if CSV files cannot be found - e.g. when running from monorepo root via turbo
# CSV_UPLOAD_DIR=/path/to/payload-turbo-app/apps/cms/csv-uploads

# Migration session API: shared secret for content-rank app to create/update review sessions (optional).
# Set the same value in content-rank's MIGRATION_SESSION_API_SECRET when using the Content Migration Analyzer.
# MIGRATION_SESSION_API_SECRET=your-shared-secret
